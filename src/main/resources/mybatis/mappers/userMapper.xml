<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oneteam.dormease.user.member.IUserMapper">
    <select id="selectDuplicateByID" parameterType="String" resultType="boolean">
        select
            count(*)
        from
            (select id
         from student
         union
         select id
         from parents) a
        where
            id = #{id}
    </select>
    <select id="selectStudentByNo" parameterType="String" resultType="com.oneteam.dormease.user.student.StudentDto">
        select
            *
        from
            student
        where
            no = #{no}
    </select>
    <select id="selectParentByNo" parameterType="String" resultType="com.oneteam.dormease.user.parents.ParentsDto">
        select
            *
        from
            parents
        where
            no = #{no}
    </select>
    <update id="updateStudentPassword" parameterType="String">
    update
        student
    set
        password = #{password}
    where
        no = #{no}
    </update>
    <update id="updateParentPassword" parameterType="String">
        update
            parents
        set
            password = #{password}
        where
            no = #{no}
    </update>
</mapper>